<!DOCTYPE html>
<html lang="<%= h(language) %>">
<head>
	<meta charset="utf-8"/>
	<title><%= h(title) %></title>
	<meta name="generator" content="Dropcaster <%= Dropcaster::VERSION%>"/>
	<meta name="keywords" content="<%= keywords.map{|k| h(k)}.join(', ') %>"/>
  <% unless copyright.blank? %>
	<meta name="copyright" content="<%= h(copyright) %>"/>
  <% end %>
  <link rel="canonical" href="<%= h(url) %>"/>
  <link rel="alternate" type="application/rss+xml" title="<%= h(title) %> Podcast Feed" href="<%= h(url) %>">
</head>

<body id="home">
	<h1><%= h(title) %></h1>
	<% unless subtitle.blank? %>
  <h2><%= h(subtitle) %></h2>
  <% end %>
  
  <% unless image_url.blank? %>
  <img src="<%= image_url %>"/>
  <% end %>
  <p><%= h(description) %></p>

  <h1>Episodes</h1>
  <% items.each{|item| %>
    <div class="item" id="<%= h(item.uuid) %>">
    <% if self[:episode_pages] %>
      <h2><a href="<%= item.page_file_name %>"><%= h(item.tag.title) || h(item.tag2.TIT2) %></a></h2>
    <% else %>
      <h2><%= h(item.tag.title) || h(item.tag2.TIT2) %></h2>
    <% end %>
    <% unless item.tag2.SUBTITLE.blank? %>
      <h3><%= h(item.tag2.SUBTITLE) %></h3>
    <% end %>
    <% unless item.tag2.TT3.blank? %>
      <p><%= h(item.tag2.TT3) %></p>
    <% end %>
      <img src="<%= item.image_url %>"/>
      <p>Download: <a href="<%= item.url %>">MP3</a> (<%= Dropcaster.humanize_time(item.duration.to_i) %>, <%= Dropcaster.humanize_size(item.file_size) %>)</p>
      <p>Published <%= h(item.pub_date.to_formatted_s(:rfc822)) %></p>
    <% unless item.keywords.blank? %>
      <p><%= item.keywords.map{|k| h(k)}.join(',') %></p>
    <% end %>
    </div>
  <% } %>
</body>
</html>
